# 3D Pipe Routing Project - 필요한 패키지들
# 이 파일은 프로젝트 실행에 필요한 모든 Python 라이브러리를 나열합니다

# 기본 과학 계산 라이브러리
numpy>=1.21.0           # 배열 및 수학 계산
scipy>=1.7.0            # 과학 계산

# 데이터 처리
pandas>=1.3.0           # 데이터 분석 및 표 생성

# 시각화
matplotlib>=3.4.0       # 그래프 생성
seaborn>=0.11.0        # 더 예쁜 그래프
plotly>=5.0.0          # 3D 인터랙티브 시각화

# 최적화 솔버 (MiniZinc 대신 Python으로 구현 가능)
# MiniZinc Python bindings (선택사항)
# minizinc>=0.4.0

# OR-Tools (Google의 최적화 라이브러리 - Gurobi 대안)
ortools>=9.0           # 무료 MIP 솔버

# 유틸리티
tqdm>=4.62.0           # 진행률 표시
pytest>=6.2.0          # 테스트
